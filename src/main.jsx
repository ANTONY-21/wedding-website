import React from "react";
import ReactDOM from "react-dom/client";
import WeddingApp from "../wedding-invitation.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WeddingApp />
  </React.StrictMode>
);
